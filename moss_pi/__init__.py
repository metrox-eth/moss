"""moss_pi - drive MOSS from a Raspberry Pi through the ESP32-S3 firmware, without dimOS.

Only the standard library is needed to import this package; pyserial (and
pygame for a gamepad) are imported where a real port or pad is opened.
"""
